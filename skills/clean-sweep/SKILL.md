---
name: clean-sweep
description: Run the Clean Code checker on the working tree or a given path and fix what it reports. Only when explicitly invoked.
---
Run `python3 "$HOME/.clean-code/clean_check.py" files <paths or nothing for changed files>`. Fix each reported line, keep every type, re-run until it prints `clean`. Report changes in one short list.
